const express = require('express');
const router = express.Router();

// Define user routes here or leave empty for now
// Example:
// router.get('/', (req, res) => res.send('User route'));

// Export router instance
module.exports = router;
